# Python dependencies for the NE India Hazard Red Zone Platform (SIH PS 26191)
# Install with:  pip install -r requirements.txt
# Python 3.10+ recommended.
#
# NOTE: the heaviest extras (rasterio/geopandas/fiona/osmium/xarray/netcdf4) are
# only needed for re-running the *full* feature-extraction pipeline against the
# raw datasets. Pure prediction/export/test work (predict.py, generate_*_export.py,
# tests/) needs numpy, pandas, xgboost, scikit-learn and shap.

numpy>=1.24
pandas>=2.0
xgboost>=2.0
scikit-learn>=1.3
shap>=0.42
rasterio>=1.3
geopandas>=0.14
osmium>=3.7
folium>=0.15
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
netcdf4>=1.6
xarray>=2023.1
fiona>=1.9
pyproj>=3.6
scipy>=1.11
networkx>=3.1
